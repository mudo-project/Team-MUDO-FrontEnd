# Memo Domain — CONTEXT
> 배치 경로: `src/feature/memo/CONTEXT.md`
> 목적: 이 문서를 읽은 사람 또는 AI 에이전트가 메모 도메인이 **무엇을 하는 도메인이고, 어떤 기능이 있으며, 어떤 조각으로 이루어져 있는지** 파악할 수 있게 한다.
> 구현 상태: **목표 스펙 (미구현)**. 현재는 더미 데이터를 렌더링하는 정적 프로토타입만 존재하며(개발 진행되며 제거될 임시 화면), 아래 컨테이너 영속성·열림/닫힘 로직은 아직 구현되지 않았다.

---

## 1. 개요

사용자 개인이 사용하는 메모 도메인.

다른 도메인에서 작업하는 도중에도 메모를 작성·조회할 수 있어야 한다는 것이 이 도메인의 핵심 목적이다. 따라서 메모는 독립된 페이지가 아니라 **화면 우측에 떠 있는 컨테이너** 형태로 제공된다.

### 핵심 제약

- 메모 컨테이너는 한 번 열리면 **사용자가 다른 페이지로 이동해도 계속 유지된다.**
- 컨테이너가 닫히는 경우는 두 가지뿐이다.
  1. 컨테이너 우상단의 X 버튼 클릭
  2. Sidebar의 메모 메뉴 재클릭
- 즉 라우트 이동은 컨테이너의 열림/닫힘 상태에 영향을 주지 않는다.

### 진입점

`src/components/layout/Sidebar.tsx` 의 메모 메뉴. 이 메뉴 클릭이 컨테이너를 여는 유일한 진입점이다.

---

## 2. 용어

| 용어 | 정의 |
|---|---|
| **메모 컨테이너** | Sidebar 메모 클릭 시 화면 우측에 나타나는 영역 전체 |
| **메모 카드** | 메모 1건을 표시하는 단위. 컨테이너 main 영역에 그리드로 배치된다 |
| **케밥 메뉴** | 메모 카드 안의 `⋮` 버튼과 그 하위 메뉴 (수정 / 색상 변경 / 삭제) |
| **표시 모드** | 메모 카드가 내용을 보여주기만 하는 상태 |
| **편집 모드** | 메모 카드에서 제목·내용·색상을 입력/수정할 수 있는 상태 |
| **정렬 필터** | Nav 바의 최신순 / 오래된순 선택 |

---

## 3. 화면 구성

메모 컨테이너는 Header / Nav / Main 3단 구조다.

```
┌─ 메모 컨테이너 ─────────────────────────────┐
│ [Header]                                    │
│  메모   (메모 갯수)      [새 메모]   [X]     │
├─────────────────────────────────────────────┤
│ [Nav]                                       │
│  [최신순] [오래된순]        현재 필터 상태    │
├─────────────────────────────────────────────┤
│ [Main]                                      │
│  ┌─ 메모 카드 ─┐  ┌─ 메모 카드 ─┐            │
│  │ 제목        │  │ 제목        │            │
│  │ 내용        │  │ 내용        │            │
│  │ 날짜     ⋮  │  │ 날짜     ⋮  │            │
│  └─────────────┘  └─────────────┘            │
└─────────────────────────────────────────────┘
```

### Header

| 요소 | 설명 |
|---|---|
| 메모 | 컨테이너 제목 |
| 메모 갯수 | 현재 메모 개수 표시 |
| 새 메모 버튼 | 새 메모 카드 생성 |
| X 버튼 | 컨테이너 닫기 |

### Nav

| 요소 | 설명 |
|---|---|
| 최신순 / 오래된순 | 메모 카드 정렬 필터 |
| 현재 필터 상태 | Nav 우측에서 현재 어떤 정렬이 적용 중인지 안내 |

### Main

- 메모 카드 그리드.
- 기본 **한 라인에 2개**씩 배치.
- **768px 이하에서는 한 라인에 1개**씩 배치.

### 메모 카드 구성 요소

- 메모 제목
- 메모 내용
- 날짜
- 케밥 메뉴

---

## 4. 기능 목록

### 4.1 컨테이너

| 기능 | 트리거 | 동작 |
|---|---|---|
| 컨테이너 열기 | Sidebar 메모 클릭 | 화면 우측에 메모 컨테이너 표시 |
| 컨테이너 닫기 | X 버튼 클릭 | 컨테이너 숨김 |
| 컨테이너 닫기 | Sidebar 메모 재클릭 | 컨테이너 숨김 |
| 컨테이너 유지 | 페이지 이동 | 컨테이너가 닫히지 않고 그대로 유지 |

### 4.2 메모 CRUD

| 기능 | 트리거 | 동작 |
|---|---|---|
| 새 메모 생성 | Header `새 메모` 클릭 | 메모 카드가 생성되고 제목·내용·색상을 입력할 수 있는 편집 모드로 진입 |
| 메모 수정 | 케밥 메뉴 → `수정` | 해당 카드가 편집 모드로 전환되어 제목·내용·색상을 수정할 수 있음 |
| 색상 변경 | 케밥 메뉴 → `색상 변경` | 해당 카드의 색상을 변경할 수 있음 |
| 메모 삭제 | 케밥 메뉴 → `삭제` | 해당 메모를 삭제 |

### 4.3 정렬

| 기능 | 트리거 | 동작 |
|---|---|---|
| 최신순 정렬 | Nav `최신순` 클릭 | 메모 카드를 최신순으로 정렬 |
| 오래된순 정렬 | Nav `오래된순` 클릭 | 메모 카드를 오래된순으로 정렬 |

### 4.4 케밥 메뉴

| 기능 | 트리거 | 동작 |
|---|---|---|
| 메뉴 열기 | 카드 내 `⋮` 클릭 | 수정 / 색상 변경 / 삭제 항목 표시 |

---

## 5. 데이터

메모 1건이 가지는 정보.

| 항목 | 설명 |
|---|---|
| 제목 | 메모 카드 상단에 표시 |
| 내용 | 메모 본문 |
| 색상 | 메모 카드의 색상. 생성·수정·색상 변경에서 지정 |
| 날짜 | 메모 카드 하단에 표시. 정렬 기준이 되는 값 |

---

## 6. 컴포넌트 구성

기능 단위로 분해했을 때 필요한 컴포넌트 목록과 각자의 책임.

| 컴포넌트 | 책임 |
|---|---|
| **MemoContainer** | 메모 컨테이너 셸. 라우트와 무관하게 항상 마운트되는 상위 레이아웃 계층에 위치하며 Header / Nav / Main 배치와 열림·닫힘 상태를 담당. Header는 별도 컴포넌트로 분리하지 않고 이 안에 직접 작성 |
| **MemoCreateForm** | 메모 작성. 새 메모 생성 시 제목·내용·색상 입력 |
| **MemoEditForm** | 메모 수정. 기존 메모의 제목·내용·색상 수정 |
| **MemoFilter** | 정렬 필터(최신순 / 오래된순)와 현재 필터 상태 표시 |
| **MemoCard** | 메모 1건의 표시 모드. 제목·내용·날짜·케밥 메뉴 |
| **MemoCardMenu** | 케밥 메뉴. 수정 / 색상 변경 / 삭제 항목 |
| **MemoColorPicker** | 색상 선택 UI. `MemoRegistForm`, `MemoEditForm`이 공유 |

> `approval` 도메인이 생성/수정 모달을 분리(`CreateApprovalModal` / `UpdateApprovalModal`)해 쓰는 것과 동일하게, 메모도 작성 폼과 수정 폼을 분리하는 구조로 확정.

### 관계

```
MemoContainer
├── MemoFilter
└── [메모 카드 목록]
    ├── MemoCard                 (표시 모드)
    │   └── MemoCardMenu         (수정 / 색상 변경 / 삭제 트리거)
    ├── MemoCreateForm           (새 메모 생성 시)
    │   └── MemoColorPicker
    └── MemoEditForm             (케밥 메뉴 → 수정 선택 시)
        └── MemoColorPicker
```

### 컴포넌트 외 필요한 조각

| 조각 | 역할 |
|---|---|
| 컨테이너 열림 상태 | 페이지 이동과 무관하게 유지되어야 하므로 페이지 단위가 아닌 상위 레이아웃 계층에서 관리 |
| 정렬 상태 | 현재 선택된 정렬 필터 |
| 메모 데이터 | 메모 목록 조회 및 CRUD |

---

## 7. 상태 정리

| 상태 | 값 |
|---|---|
| 컨테이너 | 열림 / 닫힘 |
| 정렬 | 최신순 / 오래된순 |
| 메모 카드 | 표시 모드 / 편집 모드 |
| 케밥 메뉴 | 열림 / 닫힘 |

---