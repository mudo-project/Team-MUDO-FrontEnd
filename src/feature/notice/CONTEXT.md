# Notice(공지사항) Domain — CONTEXT
> 배치 경로: `src/feature/notice/CONTEXT.md`
> 목적: 이 문서를 읽은 사람 또는 AI 에이전트가 공지사항 도메인이 **무엇을 하는 도메인이고, 어떤 기능이 있으며, 어떤 조각으로 이루어져 있는지** 파악할 수 있게 한다.

---

## 1. 개요

전체 구성원에게 전달할 공지를 등록·조회하는 도메인.

작성자가 공지를 등록하면 모든 사용자가 목록에서 확인할 수 있고, 중요한 공지는 상단 고정해 강조할 수 있다.

### 핵심 제약

- 공지 작성 시 **제목·내용은 필수값**, **첨부파일·상단 고정은 선택값**이다.
- 상단 고정된 공지는 목록 최상단에 위치하며 중요 표시·강조 표시가 함께 나타난다.
- 목록 화면의 `공지 작성` 버튼은 `NOTICE:WRITE` 권한이 있어야 노출된다(`useUserStore`의 `permissions`).
- 상세조회 화면의 고정 버튼은 `NOTICE:PIN` 권한이 있어야 노출된다.
- 상세조회 화면의 수정/삭제 버튼은 **작성자에게만** 노출된다(`getCurrentUserIdAction`으로 얻은 로그인 사용자 id와 `authorUserId`를 비교).

### 진입점

Sidebar의 공지사항 메뉴(`src/components/layout/Sidebar.tsx`, `href: "/notice"`). 라우트는 `src/app/(user)/notice/page.tsx`.

### 데이터 연동 계층

- `src/feature/notice/type.ts` — 요청/응답 인터페이스. `export` 없이 선언되어 프로젝트 전역에서 import 없이 바로 참조된다(다른 도메인 `type.ts`와 다른 스타일이니 이 파일에 타입을 추가할 때는 기존 방식을 따른다).
- `src/service/notice.service.ts` — `fetchWithAuth` 기반 API 호출(`getNoticeList`, `createNotice`, `getNoticeDetail`, `getNoticeReaders`, `updateNotice`, `deleteNotice`, `pinNotice`, `unpinNotice`).
- `src/feature/notice/actions.ts` — 위 service를 감싼 Server Action(`getNoticeListAction` 등). 컴포넌트는 이 액션만 호출하고 service를 직접 부르지 않는다.
- API 응답이 없거나 실패하면 목록은 "공지 목록을 불러오지 못했습니다", 상세조회는 "공지를 찾을 수 없습니다"를 보여준다(페이지 자체는 죽지 않음).

---

## 2. 용어

| 용어 | 정의 |
|---|---|
| **공지 목록** | 등록된 공지를 나열하는 화면. 상단고정 여부·제목·파일첨부 여부·작성자·날짜를 표시 |
| **상단 고정 공지** | 고정 여부가 참인 공지. 목록 최상단에 위치하고 중요 표시·강조 표시가 붙음 |
| **공지 작성 모달** | 제목·내용·첨부파일·상단 고정 여부를 입력하는 모달 |
| **공지 수정 모달** | 작성 모달과 동일한 구성(제목·내용·상단고정)에 기존 값이 채워진 상태로 열리는 모달 |
| **공지 상세조회** | 목록에서 공지를 클릭했을 때 나타나는 단건 상세 화면 |
| **이전 공지 / 다음 공지** | 상세조회 하단에서 인접한 공지로 이동하는 네비게이션. 대상이 없으면 안내 문구 표시 |

---

## 3. 화면 구성

공지사항 도메인은 **목록 화면**과 **상세조회 화면** 두 개로 구성된다.

### 목록 화면

```
┌─ 공지 목록 화면 ────────────────────────────────┐
│ [Header]                                        │
│                        [검색]   [공지 작성]      │
├──────────────────────────────────────────────────┤
│ [목록]                                           │
│  고정 | 제목            | 첨부 | 작성자 | 날짜   │
│  📌   | (중요)제목A     | 📎   | 김지수  | 08.01 │
│       | 제목B           |      | 정다은  | 07.30 │
└──────────────────────────────────────────────────┘
```

- 검색 결과가 없으면 "검색 결과가 없습니다" 표시.
- 등록된 공지가 없으면 "등록된 공지가 없습니다" 표시.
- API 호출 자체가 실패하면 "공지 목록을 불러오지 못했습니다" 표시.

### 상세조회 화면

```
┌─ 공지 상세조회 화면 ─────────────────────────────┐
│ [목록으로]                                       │
├───────────────────────────────────────────────────┤
│ [Header]                                          │
│  (고정 표시) [고정 등록/수정/삭제]* 작성자 작성일 │
│  조회수  읽음수                                   │
│  * 고정: NOTICE:PIN 권한 / 수정·삭제: 작성자만 노출│
├───────────────────────────────────────────────────┤
│ [본문]                                            │
│  공지 내용                                        │
│  첨부파일 (개수)                                  │
│   - 파일명   [다운로드]                           │
├───────────────────────────────────────────────────┤
│ [Footer]                                          │
│  ◀ 이전 공지: 제목 / 없으면 "이전 공지가 없습니다"│
│  ▶ 다음 공지: 제목  / 없으면 "다음 공지가 없습니다"│
└───────────────────────────────────────────────────┘
```

---

## 4. 기능 목록

### 4.1 목록 조회

| 기능 | 트리거 | 동작 | 상태 |
|---|---|---|---|
| 목록 진입 | Sidebar 공지사항 클릭 | `/notice` 이동, `getNoticeListAction`으로 목록 조회 | 구현 완료 |
| 상세조회 이동 | 목록 항목 클릭 | 해당 공지의 `/notice/[id]`로 이동 | 구현 완료 |
| 첨부 표시 | 공지에 첨부파일 존재(`hasAttachment`) | 파일첨부 이모지 표시 | 구현 완료 |
| 고정 강조 | 공지의 `pinned`가 참 | 중요 표시 + 강조 표시. 목록 정렬(상단 배치)은 API가 내려주는 순서를 그대로 따름 | 구현 완료 |
| 빈 목록 안내 | 조회된 공지 0건 | 검색어가 있으면 "검색 결과가 없습니다", 없으면 "등록된 공지가 없습니다" | 구현 완료 |
| 조회 실패 안내 | 목록 조회 실패 | "공지 목록을 불러오지 못했습니다" 표시 | 구현 완료 |

### 4.2 검색

| 기능 | 트리거 | 동작 |
|---|---|---|
| 제목 검색 | 상단 검색 입력 후 Enter | `/notice?keyword=...`로 이동, 서버에서 `getNoticeListAction({ keyword })` 호출 |
| 검색어 유지 | 검색 후 새로고침 | 검색창에 현재 검색어가 그대로 유지됨 |
| 검색 결과 없음 안내 | 검색 결과 0건 | "검색 결과가 없습니다" 표시 |

### 4.3 공지 작성

| 기능 | 트리거 | 동작 | 상태 |
|---|---|---|---|
| 작성 버튼 노출 제어 | — | `NOTICE:WRITE` 권한이 없으면 `공지 작성` 버튼이 렌더링되지 않는다(`NoticeCreateForm`) | 구현 완료 |
| 작성 모달 열기/닫기 | 상단 `공지 작성` 클릭 / X·취소·오버레이 클릭 | 제목·내용·첨부파일·상단 고정 입력 모달 표시/닫힘 | 구현 완료 |
| 파일 첨부/미리보기/삭제 | 첨부 영역 클릭 후 파일 선택 / 미리보기 / X | 선택한 파일을 목록에 추가/미리보기/제거 | 구현 완료 |
| 모달 내부 스크롤 | 첨부파일이 많아 모달 내용이 길어짐 | 모달이 화면 밖으로 나가지 않고 내부에서 스크롤(스크롤바는 숨김) | 구현 완료 |
| 필수값 검증 | 모달 등록 시 | 제목·내용 미입력 시 필드 아래 에러 메시지 표시 | 구현 완료 |
| 공지 등록 저장 | 모달 `등록` 클릭 (검증 통과 시) | `createNoticeAction`으로 등록, 성공 시 모달 닫힘 + 목록 갱신, 실패 시 에러 메시지 표시(모달 유지) | 구현 완료 |
| 첨부파일 서버 전송 | 공지 등록 시 | `src/feature/file`의 `uploadFiles()`로 presigned URL 발급 → S3 PUT → 파일 등록을 거쳐 `fileId`를 받고, `{fileId, fileName, fileType}` 배열을 `attachments`로 실어 `createNoticeAction` 호출 | 구현 완료 |

### 4.4 상세조회

| 기능 | 트리거 | 동작 | 상태 |
|---|---|---|---|
| 상세 진입 | 목록 항목 클릭 | `/notice/[id]`에서 해당 공지를 조회, 없으면 "공지를 찾을 수 없습니다" | 구현 완료 |
| 목록 복귀 | 상세조회 최상단 `목록으로` 클릭 | `/notice`로 이동 | 구현 완료 |
| 고정 표시 | 해당 공지가 상단 고정 상태 | 상세조회 상단에 고정 배지 표시 | 구현 완료 |
| 조회수/읽음수 표시 | 상세 진입 | 조회수, 읽음수(`N/전체`)를 노출 | 구현 완료 |
| 이전/다음 계산 | 상세 진입 | 목록에서 현재 공지의 앞/뒤 항목을 찾아 표시 | 구현 완료 (한 번에 조회하는 목록 범위 내에서만 계산 — 전용 이전/다음 API는 없음) |
| 고정 버튼 노출 제어 | — | `NOTICE:PIN` 권한이 있을 때만 고정 아이콘 노출 | 구현 완료 |
| 수정/삭제 버튼 노출 제어 | — | 로그인 사용자 id와 `authorUserId`가 같을 때(작성자)만 수정/삭제 아이콘 노출 | 구현 완료 |

### 4.5 수정 / 삭제

| 기능 | 트리거 | 동작 | 상태 |
|---|---|---|---|
| 수정 모달 열기 | 상세조회 `연필` 아이콘 클릭 | 작성 모달과 동일한 구성(제목·내용·상단고정·기존 첨부파일)의 모달이 값이 채워진 상태로 열림 | 구현 완료 |
| 수정 필수값 검증 | 수정 모달 `수정` 클릭 | 제목·내용 미입력 시 에러 표시 | 구현 완료 |
| 수정 결과 저장 | 검증 통과 시 | 제목·내용·첨부파일은 하나의 수정 API로, 상단 고정 변경은 별도 API로 나눠 저장(제목·내용 수정 API가 상단 고정은 받지 않음). 모두 성공해야 모달이 닫히고 화면이 갱신됨 | 구현 완료 |
| 수정 시 첨부파일 변경 | 수정 모달에서 파일 추가/기존 파일 삭제 후 `수정` 클릭 | 새로 추가한 파일만 `uploadFiles()`로 업로드해 `fileId`를 얻고, 삭제되지 않고 남아있는 기존 첨부(`fileId`)와 합쳐 전체 목록을 `attachments`로 수정 API에 항상 명시적으로 실어 보냄(부분 추가/삭제가 아닌 전체 목록 치환; 첨부파일을 전부 지운 경우에도 `attachments: []`를 그대로 보내야 서버가 삭제로 반영한다 — 필드를 생략하면 서버는 기존 첨부파일을 그대로 유지한다) | 구현 완료 |
| 삭제 | 상세조회 `휴지통` 아이콘 클릭 | 공지 삭제 후 목록으로 이동 | 구현 완료 |

### 4.6 상단 고정 등록/해제

| 기능 | 트리거 | 동작 | 상태 |
|---|---|---|---|
| 고정 토글 | 상세조회 `핀` 아이콘 클릭 | 고정/비고정을 전환하고 목록에도 반영 | 구현 완료 |

### 4.7 첨부파일 다운로드

| 기능 | 트리거 | 동작 |
|---|---|---|
| 파일 다운로드 | 상세조회 파일 목록의 다운로드 아이콘 클릭 | 해당 파일을 새 탭에서 다운로드 |

### 4.8 이전/다음 공지 이동

| 기능 | 트리거 | 동작 | 상태 |
|---|---|---|---|
| 이전 공지 이동 | 하단 이전 공지 제목 클릭 | 이전 공지 상세조회로 이동 | 구현 완료 |
| 다음 공지 이동 | 하단 다음 공지 제목 클릭 | 다음 공지 상세조회로 이동 | 구현 완료 |
| 대상 없음 안내 | 이전/다음 공지가 없음 | "이전 공지가 없습니다" / "다음 공지가 없습니다" 표시 | 구현 완료 |

---

## 5. 데이터

`src/feature/notice/type.ts`에 정의된 타입을 사용한다.

### 목록 (`NoticeListItemData`)

| 항목 | 필드명 | 설명 |
|---|---|---|
| 아이디 | `id` | 상세조회 라우팅(`/notice/[id]`) |
| 제목 | `title` | 목록 표시, 검색 기준값 |
| 작성자 | `authorName`, `authorRole` | 목록에는 `authorName`만 표시 |
| 상단 고정 여부 | `pinned` | 참이면 중요 표시 |
| 읽음 여부 | `read` | 목록 UI에는 표시하지 않음 |
| 첨부 여부 | `hasAttachment` | 참이면 클립 아이콘 표시 |
| 작성일 | `createdAt` | 목록에는 `MM.dd` 형태로 표시 |

### 상세조회 (`NoticeDetailData`)

| 항목 | 필드명 | 설명 |
|---|---|---|
| 아이디 | `id` | — |
| 제목/내용 | `title`, `content` | — |
| 작성자 | `authorUserId`, `authorRole`, `authorName` | `authorUserId`는 작성자 권한 판별에 쓸 필드(현재 미사용) |
| 상단 고정 여부 | `pinned` | — |
| 조회수/읽음 | `viewCount`, `readerCount`, `totalRecipientCount` | `읽음 N/전체` 형태로 표시 |
| 작성일 | `createdAt` | 상세조회에 표시(`updatedAt`은 미사용) |
| 첨부파일 | `attachments: NoticeAttachmentData[]` | 각 항목은 `id`(=`fileId`), `fileUrl`, `fileName`, `fileType`. 다운로드·이미지 미리보기는 `fileUrl`을 쓰지 않고 `id`로 `getFileDownloadUrlAction`을 호출해 받은 URL을 사용(`NoticeFileList`) |

---

## 6. 컴포넌트 구성

기능 단위로 분해했을 때 필요한 컴포넌트 목록과 각자의 책임. `src/feature/notice/components/`에 아래 컴포넌트 파일이 있다.

목록 화면은 `src/app/(user)/notice/page.tsx`, 상세조회 화면은 `src/app/(user)/notice/[id]/page.tsx`로 분리된다.

| 컴포넌트 | 책임 |
|---|---|
| **(page.tsx)** | 목록 화면 셸(서버 컴포넌트). `searchParams.keyword`로 `getNoticeListAction`을 호출해 목록을 받아온다. 검색창·작성 버튼·목록을 배치. Header를 별도 컴포넌트로 분리하지 않고 이 안에 직접 작성 |
| **NoticeSearch** | 제목 검색 입력. 기본 GET 폼 제출로 `/notice?keyword=...`로 이동한다(자바스크립트 불필요). 현재 검색어를 입력값에 유지 |
| **NoticeList** | 공지 목록(서버 컴포넌트). `notices`, `keyword`를 props로 받는다. 목록이 비어 있으면 검색어 유무에 따라 안내 문구를 보여주고, 아니면 각 항목을 링크로 감싸 `/notice/[id]`로 이동시킨다. `NoticeListItem`을 쓰지 않고 항목 마크업을 직접 그림(아래 참고) |
| **NoticeListItem** | 목록 항목 1건을 위해 준비된 컴포넌트지만 아직 쓰이지 않는 뼈대 — `NoticeList`가 항목 마크업을 직접 들고 있다 |
| **NoticeCreateForm** | 클라이언트 컴포넌트. `공지 작성` 트리거 버튼과 작성 모달을 함께 소유. 제목·내용·상단고정은 `react-hook-form` + `zod`(`src/lib/noticeCreateSchema.ts`)로 검증하고, 통과하면 `createNoticeAction`을 호출한다. `NoticeFileUpload`가 올려주는 File 목록을 `uploadFiles()`로 업로드해 받은 `fileId`를 `attachments`(`{fileId, fileName, fileType}[]`, 첨부가 없으면 인자 자체를 생략)로 실어 함께 전송한다 |
| **NoticeFileUpload** | 클라이언트 컴포넌트. `NoticeCreateForm`/`NoticeEditForm`이 공유하는 첨부파일 UI. 새로 추가한 파일은 자체 상태로 관리하며 추가/미리보기/삭제가 가능하다. `initialFiles` prop으로 이미 저장된 파일(`id`(fileId)·`name`)을 목록에 미리 채운다(이 경우 파일명만 있고 삭제만 가능, 미리보기는 불가). `onFilesChange`(새 파일 목록)·`onExistingFilesChange`(삭제 후 남은 기존 파일 목록)로 현재 상태를 부모 폼에 올려준다. 확장자 배지·크기 포맷 함수는 `src/lib/file.ts`에서 가져와 `NoticeDetail`과 공유한다 |
| **NoticeEditForm** | 클라이언트 컴포넌트. `notice`를 props로 받아 `공지 수정`(연필 아이콘) 트리거와 수정 모달을 소유. 작성 모달과 동일한 스키마(`noticeCreateSchema`)로 제목·내용·상단고정을 검증한다. 제출 시 `NoticeFileUpload`가 올려준 새 파일을 `uploadFiles()`로 업로드하고, 삭제되지 않고 남은 기존 첨부(`onExistingFilesChange`로 추적)와 합쳐 만든 전체 `attachments` 목록(빈 배열이어도 그대로)을 제목·내용과 함께 매번 명시적으로 수정 API에 보낸다. 상단 고정은 (값이 바뀐 경우에만) 별도로 고정/고정해제 API를 호출한다 |
| **NoticeDetailToolbar** | 클라이언트 컴포넌트. `notice`를 props로 받아 고정 배지 + 고정/수정/삭제 아이콘 버튼 행을 소유한다. 고정 배지와 핀 버튼이 같은 상태를 공유해야 해서 `NoticeDetail`에서 분리된 하나의 클라이언트 컴포넌트로 묶여 있다. 핀 아이콘 클릭 시 고정/고정해제 API 호출 결과를 반영하고, 삭제 아이콘 클릭 시 공지를 삭제하고 목록으로 이동한다. `NoticeEditForm`을 이 버튼 그룹 안에 둔다 |
| **NoticeDetail** | 상세조회 화면 본문(서버 컴포넌트). `id`를 props로 받아 공지를 조회하고(없으면 "공지를 찾을 수 없습니다"), 목록에서 이전/다음 항목을 계산한다. 목록으로 링크, `NoticeDetailToolbar`, 작성자·작성일·조회수·읽음수, 본문, 첨부파일 목록, 이전/다음 네비게이션을 전부 이 컴포넌트 안에서 직접 그린다 |
| **NoticeFileList** | 클라이언트 컴포넌트. `attachments`를 props로 받아 첨부파일 목록을 그린다. 이미지 첨부는 마운트 시 각 항목의 `id`(fileId)로 `getFileDownloadUrlAction`을 호출해 받은 URL로 `<img>` 미리보기를 띄우고, 그 외 파일은 파일명 + 다운로드 아이콘(클릭 시 같은 방식으로 URL을 조회해 새 탭으로 연다) |
| **NoticeNavigation** | 준비된 컴포넌트지만 아직 쓰이지 않는 뼈대 — `NoticeDetail`이 이전/다음 링크를 직접 그린다 |

> `memo` 도메인이 작성 폼과 수정 폼을 `MemoCreateForm` / `MemoEditForm`으로 분리한 것과 동일하게, 공지도 작성 폼과 수정 폼을 분리하는 구조다.
> `memo` 도메인의 `MemoContainer`가 Header를 별도 컴포넌트로 분리하지 않은 것과 동일하게, 목록 화면(`page.tsx`)과 `NoticeDetail`도 각자의 Header 영역을 내부에 직접 작성한다.
> `NoticeFileUpload`(작성/수정 폼의 첨부 UI)와 `NoticeFileList`(상세조회의 읽기 전용 목록)는 이름이 비슷하지만 역할이 다른 별개 컴포넌트다.
> `NoticeListItem`, `NoticeNavigation`은 파일은 있지만 `NoticeList`/`NoticeDetail`이 해당 마크업을 직접 그리고 있어 실제로 쓰이지 않는다. 각 화면이 복잡해지면 그때 분리한다.
> `NoticeCreateForm`/`NoticeEditForm`은 `react-hook-form`의 `handleSubmit` 안에서 서버 액션을 직접 호출하는 방식을 쓴다(`<form action={...}>` + `useActionState` 방식이 아님) — zod 검증을 폼에서 직접 제어해야 하기 때문이다.
> `NoticeDetailToolbar`의 `pinned` 상태는 `notice.pinned` prop이 바뀌면 렌더링 중에 동기화된다(수정 모달에서 상단 고정을 바꾼 뒤에도 배지가 최신 상태를 반영하도록).

### 관계

```
notice/page.tsx                    (목록 화면, /notice, searchParams.keyword로 검색)
├── NoticeSearch                   (GET 폼, keyword 유지)
├── NoticeList                     (각 항목이 Link로 /notice/[id] 이동)
└── NoticeCreateForm                (작성 버튼 클릭 시)
    └── NoticeFileUpload

notice/[id]/page.tsx               (상세조회 화면, /notice/[id])
└── NoticeDetail                   (공지 조회 + 이전/다음 계산)
    └── NoticeDetailToolbar        (고정 토글, 삭제)
        └── NoticeEditForm         (수정 아이콘 클릭 시)
            └── NoticeFileUpload   (기존 첨부파일 미리 채움)
```

### 컴포넌트 외 필요한 조각

| 조각 | 역할 | 상태 |
|---|---|---|
| 공지 데이터 | 목록·상세조회·CRUD | 구현 완료 — `actions.ts` + `notice.service.ts`로 연동 |
| 검색 상태 | 현재 검색어, 필터링된 목록 | 구현 완료 — URL의 `searchParams.keyword`로 관리 |
| 첨부파일 업로드 | 새 파일을 서버에 올려 `fileId`를 받아오는 절차 | 구현 완료 — `src/feature/file`의 presigned URL 발급 → S3 PUT → 파일 등록 헬퍼(`uploadFiles()`) 재사용, 작성·수정 모두에서 사용 |
| 권한/작성자 판별 | 작성·고정·수정·삭제 버튼 노출 여부 결정 | 구현 완료 — 작성은 `NOTICE:WRITE`, 고정은 `NOTICE:PIN`(`useUserStore`), 수정/삭제는 작성자 여부(`getCurrentUserIdAction`)로 판별 |

---

## 7. 상태 정리

| 상태 | 값 |
|---|---|
| 화면 | 목록 / 상세조회 |
| 공지 작성·수정 모달 | 열림 / 닫힘 |
| 상단 고정 | 고정 / 비고정 |
| 검색 | 검색 전 / 검색 결과 있음 / 검색 결과 없음 |
| 데이터 로딩 | 정상 / 조회 실패(목록: 불러오기 실패 문구, 상세: 공지 없음 문구) |

---
